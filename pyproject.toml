[project]
name = "f1-channels-app"
version = "0.1.0"
description = "Formula 1 Live Channels Streaming Hub"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "Flask>=3.0.0",
    "Flask-Cors>=4.0.0"
]
